# ==========================
# Environment
# ==========================
# Für lokale Entwicklung: development, für Live: production
NODE_ENV=development

# Öffentlich erreichbare Seite
SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ==========================
# Server (PHP-Backend)
# ==========================
SERVER_BASE_URL=https://tubox.de/TUBOX/server
NEXT_PUBLIC_SERVER_BASE_URL=${SERVER_BASE_URL}

# Öffentliche Upload-URL und Endpunkte
NEXT_PUBLIC_UPLOADS_BASE_URL=${NEXT_PUBLIC_SERVER_BASE_URL}/uploads
NEXT_PUBLIC_UPLOAD_ENDPOINT=${NEXT_PUBLIC_SERVER_BASE_URL}/upload.php

# Token, das im Browser mitgesendet werden darf (keine Geheimnisse!)
NEXT_PUBLIC_SERVER_API_TOKEN=your_public_browser_token

# ==========================
# Externe Blog-Lese/Schreibquelle (PHP)
# ==========================
# Wenn true, werden Blogdaten extern über deinen PHP-Webspace gelesen/geschrieben
USE_EXTERNAL=true
# Externes Blog-JSON-Verzeichnis (index.json + einzelne Posts)
EXTERNAL_BLOG_URL=${NEXT_PUBLIC_SERVER_BASE_URL}/uploads/blog
# PHP-Endpoint zum Aktualisieren/Löschen von Blogposts/Index
UPDATE_BLOG_PHP_URL=${SERVER_BASE_URL}/update-blog.php

# ==========================
# Admin Login (ohne DB)
# ==========================
ADMIN_EMAIL=admin@example.com
ADMIN_USERNAME=admin
ADMIN_DISPLAY_NAME=Admin
# ENTWEDER: Klartext-Passwort (nur für Entwicklung)
# ADMIN_PASSWORD=changeme
# ODER: Bcrypt-Hash (empfohlen für Produktion)
# ADMIN_PASSWORD_HASH=$2a$10$XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX